Str() = retorna nNumber, então é uma FUNÇÃO.
QOut() = tem retorno NIL, só exibe a expressão no console, logo é um PROCEDIMENTO.
Date() = retorna a data atual do sistema, então é uma FUNÇÃO.
Len() = retorna o tamanho de uma string ou array, então é uma FUNÇÃO.
Upper() = retorna uma copia de uma string convertida em maiúscula, então é uma FUNÇÃO.
ClearScreen() = (Não encontrei na documentação) Provavelmente deve retornar NIL, apenas limpando o console, logo é um PROCEDIMENTO.